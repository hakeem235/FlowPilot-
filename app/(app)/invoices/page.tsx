import { Plus, Send } from "lucide-react";import { Badge, Button, Card, PageHeading } from "@/components/ui";import { invoices, money } from "@/lib/demo-data";
export default function Invoices(){return <><PageHeading eyebrow="Receivables" title="Invoices" action={<Button><Plus size={15}/>Create invoice</Button>}>SAR 130,700 outstanding across five invoices.</PageHeading><div className="metrics"><Card className="metric"><small>OUTSTANDING</small><strong>SAR 130,700</strong><p>5 open invoices</p></Card><Card className="metric"><small>OVERDUE</small><strong>SAR 46,700</strong><p>2 need attention</p></Card><Card className="metric"><small>EXPECTED THIS MONTH</small><strong>SAR 59,100</strong><p>Probability adjusted</p></Card><Card className="metric"><small>AVERAGE PAYMENT</small><strong>24 days</strong><p>2 days faster than last quarter</p></Card></div><Card className="table-card" style={{marginTop:14} as never}><div className="tabs"><button className="active">All invoices</button><button>Outstanding</button><button>Overdue</button><button>Paid</button><button>Drafts</button></div><div className="table-scroll"><table><thead><tr><th>Invoice</th><th>Client</th><th>Status</th><th>Due</th><th>Forecast confidence</th><th className="amount">Amount</th><th></th></tr></thead><tbody>{invoices.map(i=><tr key={i.number}><td><strong>{i.number}</strong></td><td>{i.client}</td><td><Badge tone={i.status==="Overdue"?"risk":i.status==="Due soon"?"warn":"neutral"}>{i.status}</Badge></td><td>{i.due}</td><td>{i.probability}%</td><td className="amount">{money.format(i.amount)}</td><td><Button variant="ghost"><Send size={13}/>Remind</Button></td></tr>)}</tbody></table></div></Card></>}
