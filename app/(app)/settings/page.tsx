import { Button, Card, PageHeading } from "@/components/ui";
export default function Settings(){return <><PageHeading eyebrow="Workspace" title="Settings" action={<Button>Save changes</Button>}>Control forecast assumptions, team access, and account data.</PageHeading><div className="tabs"><button className="active">Forecast</button><button>Business</button><button>Team & roles</button><button>Data & privacy</button><button>Audit log</button></div><div className="settings-grid"><Card><div className="card-header"><div><h2>Planning defaults</h2><p>Used in new forecasts and safe-to-spend calculations.</p></div></div><div className="field"><label htmlFor="currency">Currency</label><select id="currency"><option>Saudi riyal (SAR)</option><option>US dollar (USD)</option><option>UAE dirham (AED)</option></select></div><div className="field"><label htmlFor="buffer">Minimum operating buffer</label><input id="buffer" defaultValue="30,000"/></div><div className="field"><label htmlFor="tax">Estimated tax reserve</label><input id="tax" defaultValue="15%"/></div><p className="helper">Tax reserves are planning estimates and do not constitute tax advice.</p></Card><Card><div className="card-header"><div><h2>Invoice confidence</h2><p>Adjust how open invoices contribute to expected cash.</p></div></div>{[["Not due","80%"],["1–15 days overdue","60%"],["16–30 days overdue","40%"],["More than 30 days overdue","20%"]].map(x=><div className="field" key={x[0]}><label>{x[0]}</label><input defaultValue={x[1]}/></div>)}</Card><Card><div className="card-header"><div><h2>Workspace members</h2><p>Roles are enforced for every organization-scoped action.</p></div><Button variant="secondary">Invite member</Button></div>{[["Ahmed Hakeem","Owner"],["Lina Kareem","Admin"],["Omar Saleh","Viewer"]].map(x=><div className="assumption" key={x[0]}><span><b>{x[0]}</b><span>{x[1]}</span></span></div>)}</Card><Card><div className="card-header"><div><h2>Data controls</h2><p>Export a portable copy or permanently close the account.</p></div></div><Button variant="secondary">Export all data</Button><div className="assumption"><b>Delete workspace</b><p>This permanently removes financial records after a confirmation period.</p><Button variant="secondary">Start deletion</Button></div></Card></div></>}
