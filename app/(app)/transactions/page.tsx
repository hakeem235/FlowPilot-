import { Download, Plus, SlidersHorizontal } from "lucide-react";
import { Button, PageHeading, Card } from "@/components/ui";
import { money, transactions } from "@/lib/demo-data";
export default function Transactions(){return <><PageHeading eyebrow="Cash ledger" title="Transactions" action={<Button><Plus size={15}/>Add transaction</Button>}>Review, search, and categorize every movement.</PageHeading><Card className="table-card"><div className="table-toolbar"><input className="filter-input" aria-label="Search transactions" placeholder="Search description or category…"/><div><Button variant="secondary"><SlidersHorizontal size={14}/>Filter</Button> <Button variant="secondary"><Download size={14}/>Import CSV</Button></div></div><div className="table-scroll"><table><thead><tr><th>Date</th><th>Description</th><th>Category</th><th>Reference</th><th className="amount">Amount</th></tr></thead><tbody>{transactions.map(t=><tr key={t.id}><td>{t.date}</td><td><strong>{t.name}</strong></td><td><span className="badge badge-neutral"><i/>{t.category}</span></td><td><small>{t.id}</small></td><td className={`amount ${t.type}`}>{t.amount>0?"+ ":"− "}{money.format(Math.abs(t.amount))}</td></tr>)}</tbody></table></div></Card></>}
