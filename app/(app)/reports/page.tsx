import { Download, FileBarChart, FileSpreadsheet, History, Landmark } from "lucide-react";import { Button, Card, PageHeading } from "@/components/ui";
const reports=[[FileBarChart,"Cash-flow forecast","Expected, optimistic, and conservative balances with assumptions."],[Landmark,"Financial position","Balances, reserves, obligations, and safe-to-spend calculation."],[FileSpreadsheet,"Transaction ledger","A categorized CSV for your accountant or bookkeeping system."],[History,"Audit activity","A record of important workspace and financial-data changes."]];
export default function Reports(){return <><PageHeading eyebrow="Exports" title="Reports" action={<Button variant="secondary">Report history</Button>}>Create a clean record for your team, accountant, or next planning session.</PageHeading><div className="report-grid">{reports.map(([Icon,title,text])=><Card className="report-card" key={title as string}><div className="report-icon"><Icon size={20}/></div><div><h3>{title as string}</h3><p>{text as string}</p><Button variant="secondary"><Download size={14}/>Export</Button></div></Card>)}</div><Card style={{marginTop:14} as never}><div className="card-header"><div><h2>Scheduled reports</h2><p>Send a monthly cash summary to workspace owners.</p></div><Button>Schedule report</Button></div><div className="empty-state"><span>+</span><h3>No scheduled reports yet</h3><p>Create one to keep your team aligned without another reminder.</p></div></Card></>}
