import { Check, Upload } from "lucide-react";import { Logo } from "@/components/logo";import { Button } from "@/components/ui";
export default function Onboarding(){return <div className="onboarding-wrap"><aside className="onboarding-side"><Logo inverse/><h1>Let’s map your cash horizon.</h1><p>We’ll start with the facts you know, then show every assumption FlowPilot adds.</p><div className="progress-list"><div className="progress-item done"><i><Check size={11}/></i>Business details</div><div className="progress-item active"><i>2</i>Starting position</div><div className="progress-item"><i>3</i>Recurring cash flow</div><div className="progress-item"><i>4</i>Open invoices</div><div className="progress-item"><i>5</i>Safety settings</div><div className="progress-item"><i>6</i>Your first forecast</div></div></aside><main className="onboarding-main"><form className="onboarding-form"><p className="eyebrow">Step 2 of 6</p><h2>Where are you starting from?</h2><p style={{color:"var(--muted)"}}>Enter today’s available bank balance, then import recent activity if you have it.</p><div className="form-grid"><div className="field full"><label htmlFor="balance">Opening bank balance</label><input id="balance" name="balance" type="number" defaultValue="186400" required/><p className="helper">Use the cleared balance across the accounts you operate from.</p></div><div className="field full"><label htmlFor="statement">Recent bank statement (optional)</label><label htmlFor="statement" className="card" style={{textAlign:"center",padding:30,cursor:"pointer"}}><Upload size={20} style={{margin:"0 auto 8px",color:"var(--green)"}}/><b>Choose a CSV file</b><p className="helper">Up to 10 MB. You can categorize transactions next.</p></label><input id="statement" type="file" accept=".csv,text/csv" hidden/></div></div><div style={{display:"flex",justifyContent:"space-between",marginTop:24}}><Button variant="secondary">Back</Button><Button href="/dashboard">Continue to recurring cash →</Button></div></form></main></div>}
